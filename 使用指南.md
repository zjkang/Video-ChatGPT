# Video-ChatGPT 使用指南

本指南说明如何下载模型后运行 demo 和评估（eval）。

## 前置准备

### 1. 环境设置

```bash
conda create --name=video_chatgpt python=3.10
conda activate video_chatgpt

cd Video-ChatGPT
pip install -r requirements.txt
export PYTHONPATH="./:$PYTHONPATH"
```

### 2. 下载模型权重

#### 下载 Video-ChatGPT 投影层权重

```bash
wget https://huggingface.co/MBZUAI/Video-ChatGPT-7B/resolve/main/video_chatgpt-7B.bin
```

或者从 HuggingFace 下载：https://huggingface.co/MBZUAI/Video-ChatGPT-7B

#### 准备 LLaVA 基础模型权重

Video-ChatGPT 基于 LLaVA，需要先准备 LLaVA 权重。

**方法1：使用 apply_delta 脚本（推荐）**

```bash
python scripts/apply_delta.py \
    --base-model-path <LLaMA 7B 权重路径> \
    --target-model-path LLaVA-Lightning-7B-v1-1 \
    --delta-path liuhaotian/LLaVA-Lightning-7B-delta-v1-1
```

**方法2：直接下载（更简单）**

从 HuggingFace 下载：https://huggingface.co/mmaaz60/LLaVA-7B-Lightening-v1-1

---

## 运行 Demo

### 方式1：Gradio Web 界面（推荐）

启动交互式 Web 界面，可以上传视频并对话：

```bash
python video_chatgpt/demo/video_demo.py \
    --model-name <LLaVA-Lightning-7B-v1-1 权重路径> \
    --projection_path <video_chatgpt-7B.bin 路径>
```

可选参数：
- `--host 0.0.0.0`：设置主机地址（默认 0.0.0.0）
- `--port 7860`：设置端口号
- `--share`：创建公共链接（可通过 Gradio 分享）

运行后，在浏览器中打开显示的 URL（通常是 `http://localhost:7860`）即可使用。

### 方式2：命令行单视频推理

对单个视频进行推理：

```bash
python -m video_chatgpt.single_video_inference \
    --model-name <LLaVA-Lightning-7B-v1-1 权重路径> \
    --projection_path <video_chatgpt-7B.bin 路径> \
    --video_path <视频文件路径>
```

运行后会提示输入问题，输入后按回车即可获得回答。

---

## 运行评估（Eval）

### 1. Video-based Generative Performance Benchmarking

评估模型在视频理解任务上的生成性能，包括5个方面：
- Correctness of Information（信息正确性）
- Detail Orientation（细节导向）
- Contextual Understanding（上下文理解）
- Temporal Understanding（时间理解）
- Consistency（一致性）

#### Step 1: 运行推理

```bash
python video_chatgpt/eval/run_inference_benchmark_general.py \
    --video_dir <视频目录路径> \
    --gt_file <ground-truth文件路径（包含QA对）> \
    --output_dir <输出目录> \
    --output_name <输出文件名> \
    --model-name <LLaVA-Lightning-7B-v1-1 权重路径> \
    --projection_path <video_chatgpt-7B.bin 路径>
```

**注意**：
- 对于 correctness、detailed orientation 和 contextual understanding，使用相同的 QA 对文件
- 对于 temporal understanding 和 consistency，需要单独的 QA 对文件

#### Step 2: 运行评估脚本

**单个评估**（例如 Correctness）：

```bash
python quantitative_evaluation/evaluate_benchmark_1_correctness.py \
    --pred_path <推理结果文件路径> \
    --output_dir <输出目录> \
    --output_json <结果JSON文件路径> \
    --api_key <OpenAI API密钥>
```

**批量评估所有5个指标**：

```bash
bash quantitative_evaluation/evaluate_benchmark.sh
```

### 2. Zero-Shot Question-Answer Evaluation

在标准 QA 数据集上评估（MSRVTT、MSVD、TGIF、ActivityNet-QA）。

#### Step 1: 运行推理（以 ActivityNet-QA 为例）

```bash
python video_chatgpt/eval/run_inference_activitynet_qa.py \
    --video_dir <视频目录路径> \
    --gt_file_question <test_q.json> \
    --gt_file_answers <test_a.json> \
    --output_dir <输出目录> \
    --output_name video_chatgpt_activitynet_qa_preds \
    --model-name <LLaVA-Lightning-7B-v1-1 权重路径> \
    --projection_path <video_chatgpt-7B.bin 路径>
```

#### Step 2: 评估结果

```bash
python quantitative_evaluation/evaluate_activitynet_qa.py \
    --pred_path <预测结果文件路径> \
    --output_dir <输出目录> \
    --output_json <结果JSON文件路径> \
    --api_key <OpenAI API密钥> \
    --num_tasks 1
```

---

## 数据集下载链接

### Demo 示例视频
下载地址：https://mbzuaiac-my.sharepoint.com/:u:/g/personal/hanoona_bangalath_mbzuai_ac_ae/Ef0AGw50jt1OrclpYYUdTegBzejbvS2-FXBCoM3qPQexTQ?e=TdnRUG

下载后放置在 `video_chatgpt/demo_sample_videos` 目录。

### Benchmark 数据集
- **视频**：https://mbzuaiac-my.sharepoint.com/:u:/g/personal/hanoona_bangalath_mbzuai_ac_ae/EatOpE7j68tLm2XAd0u6b8ABGGdVAwLMN6rqlDGM_DwhVA?e=90WIW
- **描述**：https://mbzuaiac-my.sharepoint.com/:u:/g/personal/hanoona_bangalath_mbzuai_ac_ae/EYqblLdszspJkayPvVIm5s0BCvl0m6q6B-ipmrNg-pqn6A?e=QFzc1U
- **QA对**：https://mbzuaiac-my.sharepoint.com/:f:/g/personal/hanoona_bangalath_mbzuai_ac_ae/EoS-mdm-KchDqCVbGv8v-9IB_ZZNXtcYAHtyvI06PqbF_A?e=1sNbaa

### ActivityNet-QA 数据集
- **视频**：https://mbzuaiac-my.sharepoint.com/:u:/g/personal/hanoona_bangalath_mbzuai_ac_ae/ESa302OCJMNHsMk7wuBbQc8BZH5CqlcdCWiSpXynQZDfAQ?e=CrOPbm
- **标注**：https://mbzuaiac-my.sharepoint.com/:f:/g/personal/hanoona_bangalath_mbzuai_ac_ae/El1SR1Mri2NLgptt4jTOy1wBJkGyzXDKGvsWFLxvdbpKPw?e=vxtpNu

---

## 常见问题

### GPU 内存要求
- Demo 需要约 18GB GPU 内存
- 确保有足够的显存

### 模型路径
- `--model-name`：LLaVA 基础模型路径（可以是本地路径或 HuggingFace 模型名）
- `--projection_path`：Video-ChatGPT 投影层权重路径（`video_chatgpt-7B.bin`）

### 视频格式支持
支持 `.mp4`, `.avi`, `.mov`, `.mkv` 等常见视频格式。

---

## 快速开始示例

假设你已经下载了所有模型权重：

```bash
# 1. 激活环境
conda activate video_chatgpt
cd Video-ChatGPT
export PYTHONPATH="./:$PYTHONPATH"

# 2. 运行 Gradio Demo
python video_chatgpt/demo/video_demo.py \
    --model-name ./LLaVA-Lightning-7B-v1-1 \
    --projection_path ./video_chatgpt-7B.bin

# 3. 或者运行命令行推理
python -m video_chatgpt.single_video_inference \
    --model-name ./LLaVA-Lightning-7B-v1-1 \
    --projection_path ./video_chatgpt-7B.bin \
    --video_path ./your_video.mp4
```

---

更多详细信息请参考：
- [离线 Demo 文档](docs/offline_demo.md)
- [定量评估文档](quantitative_evaluation/README.md)
- [训练文档](docs/train_video_chatgpt.md)

---

## 模型训练

### 1. 准备 LLaVA 权重
参考上文“前置准备”中的步骤，获取 LLaVA-Lightning-7B-v1-1 权重。

### 2. 准备数据集

**Step 1: 下载 VideoInstruct-100K 数据集**
下载地址：[点击下载](https://mbzuaiac-my.sharepoint.com/:u:/g/personal/hanoona_bangalath_mbzuai_ac_ae/EWxYslvDeX1PijKWM_WxTkkBDXDDD350YnUQOkbcL8V7Xg?e=Lq9itD)

**Step 2: 转换数据格式**
将下载的 JSON 转换为训练格式：

```bash
python scripts/convert_instruction_json_to_training_format.py \
    --input_json_file <下载的JSON文件路径> \
    --output_json_file video_chatgpt_training.json
```

**Step 3: 下载 ActivityNet 视频**
根据 `train_video_ids.txt` 下载视频，或者从[这里下载](https://mbzuaiac-my.sharepoint.com/:f:/g/personal/hanoona_bangalath_mbzuai_ac_ae/EnLRDehrr8lGqHpC5w1zZ9QBnsiVffYy5vCv8Hl14deRcg?e=Ul5DUE)。

**Step 4: 准备 CLIP 时空特征**
为了提高训练效率，需要预先提取视频特征。

**选项 A：自己生成（需要下载视频）**
```bash
python scripts/save_spatio_temporal_clip_features.py \
    --video_dir_path <视频目录路径> \
    --clip_feat_path <特征保存目录>
```

**选项 B：直接下载预计算特征（推荐）**
下载地址：[点击下载](https://mbzuaiac-my.sharepoint.com/:f:/g/personal/hanoona_bangalath_mbzuai_ac_ae/EnLRDehrr8lGqHpC5w1zZ9QBnsiVffYy5vCv8Hl14deRcg?e=Ul5DUE)

### 3. 启动训练

使用 8 张 A100 (40GB) 进行训练的示例命令：

```bash
torchrun --nproc_per_node=8 --master_port 29001 video_chatgpt/train/train_mem.py \
    --model_name_or_path <LLaVA-Lightning-7B-v1-1 权重路径> \
    --version v1 \
    --data_path <video_chatgpt_training.json 路径> \
    --video_folder <CLIP特征目录路径> \
    --tune_mm_mlp_adapter True \
    --mm_use_vid_start_end \
    --bf16 True \
    --output_dir ./Video-ChatGPT_7B-1.1_Checkpoints \
    --num_train_epochs 3 \
    --per_device_train_batch_size 4 \
    --per_device_eval_batch_size 4 \
    --gradient_accumulation_steps 1 \
    --evaluation_strategy "no" \
    --save_strategy "steps" \
    --save_steps 3000 \
    --save_total_limit 3 \
    --learning_rate 2e-5 \
    --weight_decay 0. \
    --warmup_ratio 0.03 \
    --lr_scheduler_type "cosine" \
    --logging_steps 100 \
    --tf32 True \
    --model_max_length 2048 \
    --gradient_checkpointing True \
    --lazy_preprocess True
```

**关键参数说明：**
- `--tune_mm_mlp_adapter True`: 仅微调多模态适配器（Projector），这是 Video-ChatGPT 的核心训练策略。
- `--video_folder`: 指向包含 `.pkl` 特征文件的目录。
- `--output_dir`: 训练后的模型权重保存位置。

更多细节请参考 [官方训练文档](docs/train_video_chatgpt.md)。

