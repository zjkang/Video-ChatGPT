# 项目研究目标与核心创新点

## Why It Works（论文最关键的 selling point）

### 问题陈述

现有视频-LLM 存在两个核心问题：
1. **成本过高**：全参数微调需要大量计算资源
2. **时序建模不足**：缺乏对视频时序信息的有效建模

### 解决方案

我们的方法使用 **"Temporal Adapter + LoRA"** 同时解决这两个问题。

---

## 三大核心创新点

### ① Temporal Adapter - 显式引入视频时序归纳偏置

- **核心优势**：明确建模视频的时序信息
- **技术对比**：比 Cat-all-patches 方法强很多
- **实现目标**：在视频特征提取和融合过程中显式引入时序建模能力

### ② LoRA Adapter - 大幅降低训练成本

- **核心优势**：显著减少训练所需的计算资源和时间
- **技术特点**：LLM 的 multi-modal alignment 不必全参微调
- **实现目标**：通过低秩适应（Low-Rank Adaptation）实现高效的多模态对齐

### ③ 通用兼容性 - 适配所有 SOTA LLM

- **支持模型**：Llama / Mistral / Qwen 等主流 LLM
- **定位**：作为 "Video-LLM 通用增强模块"
- **实现目标**：设计通用的接口和架构，确保与不同 LLM 的兼容性

---

## 开发指导原则

在代码修改和功能实现时，应始终围绕以下目标：

1. **时序建模优先**：确保 Temporal Adapter 能够有效捕获视频的时序依赖关系
2. **效率优化**：通过 LoRA 等技术最小化训练和推理成本
3. **通用性设计**：保持架构的模块化和可扩展性，支持多种 LLM 后端
4. **性能验证**：在时序理解、成本效率、兼容性等方面进行充分验证

---

## 技术路线图

- [ ] Temporal Adapter 架构设计与实现
- [ ] LoRA Adapter 集成与优化
- [ ] 多 LLM 后端支持（Llama/Mistral/Qwen）
- [ ] 时序建模能力验证
- [ ] 训练成本对比实验
- [ ] 与 Cat-all-patches 方法的性能对比

